server:
  port: 8083
spring:
  application:
    name: moto-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:moto_servicedb
    username: sa
    password: admin
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update